{
  "name": "assessment-center-api",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "rollup -c",
    "deploy": "npm run build && twilio-run deploy",
    "start:dev": "nodemon app.local.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "airtable": "^0.8.1",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "@rollup/plugin-commonjs": "11.0.2",
    "opencage-api-client": "^0.7.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.2",
    "twilio-run": "^2.0.0",
    "rollup": "^2.1.0"
  },
  "engines": {
    "node": "10.17.0"
  }
}
